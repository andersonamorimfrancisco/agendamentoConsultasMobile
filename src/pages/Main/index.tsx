import React from 'react';
import * as Styles from './styles';

interface MainProps {}

const Main = ({}: MainProps) => (
  <Styles.container>
    <Styles.Text>Main</Styles.Text>
  </Styles.container>
);

export default Main;
